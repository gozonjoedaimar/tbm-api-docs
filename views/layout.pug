doctype html
html
  head
    title= message && error && error.status ? error.status + " - " + message : title
    link(rel='stylesheet', href='/vendor.css')
    script(src="https://www.google.com/recaptcha/api.js" async defer)
  body
    block content
